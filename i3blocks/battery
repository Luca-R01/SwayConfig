#!/bin/bash

BAT=$(acpi -b | grep -E -o '[0-9][0-9][0]?%')
BAT_CHARGING=$(acpi | grep -E -o "Charging")

if [[ -z "$BAT" ]] ; then
	echo ""
else
	if [[ $BAT_CHARGING = Charging ]] ; then
        	echo "⚡🔋$BAT"
	else
        	echo "🔋$BAT"
	fi
fi



